{
  "name": "@animepaste/monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm build:cli && pnpm build:app",
    "build:all": "pnpm -r build",
    "build:app": "pnpm -C packages/app build",
    "build:cli": "pnpm -C packages/database build && pnpm -C packages/cli build",
    "build:docs": "pnpm -C docs build",
    "build:site": "optc scripts/build.ts",
    "dev": "wrangler pages dev --kv ANIME --binding DEV=true ENABLE_PUBLIC=true -- vite ./packages/app",
    "dev:docs": "pnpm -C docs dev",
    "format": "prettier --write ./**/*.{js,ts,vue}",
    "release": "bumpp package.json functions/package.json docs/package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "test": "pnpm -r test -- --run"
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "optc": "^0.3.4",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4",
    "vite": "^3.0.2",
    "vitest": "^0.19.0",
    "wrangler": "^2.0.22"
  },
  "packageManager": "pnpm@7.6.0",
  "engines": {
    "node": ">=v16.16.0"
  },
  "version": "0.0.1"
}
