datasource db {
  provider = "sqlite"
  url      = "file:./anime.db"
}

generator client {
  provider = "prisma-client-js"
}

model Resource {
  id        Int      @id @default(autoincrement())
  link      String
  type      String
  title     String   
  fansub    String
  magnet    String
  createdAt DateTime
}